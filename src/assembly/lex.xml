<assembly xmlns="http://maven.apache.org/ASSEMBLY/2.2.0"
		  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		  xsi:schemaLocation="http://maven.apache.org/ASSEMBLY/2.2.0 http://maven.apache.org/xsd/assembly-2.2.0.xsd">
	<id>lex</id>
	<formats>
		<format>zip</format>
	</formats>
	<includeBaseDirectory>false</includeBaseDirectory>

	<!-- Include the extension JAR -->
	<files>
		<file>
			<source>${project.build.directory}/${project.artifactId}-${project.version}.jar</source>
			<destName>${project.artifactId}-${project.version}.jar</destName>
			<outputDirectory>jars</outputDirectory>
		</file>
	</files>

	<!-- Include dependency JARs and extension resources -->
	<fileSets>
		<!-- BouncyCastle and Nimbus JARs -->
		<fileSet>
			<directory>${project.build.directory}/dependency-jars</directory>
			<outputDirectory>jars</outputDirectory>
			<includes>
				<include>*.jar</include>
			</includes>
		</fileSet>
		<!-- FLD -->
		<fileSet>
			<directory>${project.build.directory}/extension-resources/flds</directory>
			<outputDirectory>flds</outputDirectory>
			<includes>
				<include>*.fld</include>
			</includes>
		</fileSet>
		<!-- Manifest -->
		<fileSet>
			<directory>${project.build.directory}/extension-resources/META-INF</directory>
			<outputDirectory>META-INF</outputDirectory>
			<includes>
				<include>MANIFEST.MF</include>
			</includes>
		</fileSet>
	</fileSets>
</assembly>
